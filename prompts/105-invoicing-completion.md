# Prompt 105 — Invoicing: Complete to Category-Leading Standard


## UI/UX & ANIMATION REQUIREMENTS
Before writing any frontend code, read these skill files in full:
- /mnt/skills/user/ui-ux-pro-max/SKILL.md — apply design tokens, color palettes, font pairings, and component patterns from this skill to every page and component you create or edit
- /mnt/skills/public/frontend-design/SKILL.md — apply production-grade frontend patterns

For any page that involves data visualization, reports, charts, or animated content, also read:
- /mnt/skills/public/remotion/SKILL.md (if it exists) — use Remotion for any video/animation exports or animated report components

Apply these skills silently — do not narrate reading them. Just produce better UI as a result.
Every dashboard page must use the design system from ui-ux-pro-max: correct spacing, typography, color tokens, and component hierarchy. No plain HTML divs with inline styles that ignore the design system.

Competitor benchmark: FreshBooks, Xero invoicing, Invoice Ninja. Match 80%+ of core features.
Invoice tables exist. Some UI exists. Read ALL existing files before writing anything. Read CLAUDE.md first.

## Pre-flight (MANDATORY — read CLAUDE.md first)
```
git pull origin main
npx tsc --noEmit   # must be zero errors
npm run build      # must pass
```
Read CLAUDE.md. Read every file you will edit before touching it.
One commit per task. After every commit: git push origin main, then confirm git log origin/main..HEAD is empty.
State "Build verified green, all commits pushed." before finishing.

## UPGRADE-ONLY RULE
Never remove, stub, or downgrade any existing feature. Fix forward only.

## ARIA INTELLIGENCE RULE (applies to every task)
Every new feature must:
1. Write relevant data to aria_ai_calls (log AI usage)
2. Feed insights back into the daily briefing context (update buildAskAriaContext or daily-briefing route to include new data)
3. Log significant actions to aria_autopilot_actions
4. Use claude-haiku-4-5-20251001 unless the task requires complex reasoning (then claude-sonnet-4-5-20250929)


## DB columns (already exist — verify with list_tables before assuming)
invoices: id, business_id, customer_id, invoice_number, status, bill_to_name, bill_to_email, bill_to_address, subtotal, gst_total, total, currency, notes, issue_date, due_date, sent_at, paid_at, send_method, pdf_url, ai_generated, created_at, updated_at, viewed_at, auto_reminders
invoice_line_items: id, invoice_id, description, quantity, unit_price, tax_rate, line_total

## TASK 1 — PDF generation (serverless-safe)
Read existing /api/pos/invoices/[id]/pdf route if it exists.
If it uses full Puppeteer (not @sparticuz/chromium), fix it.
If it doesn't exist, create it.

PDF must include:
- Business logo (businesses.logo_url), name, ABN, address
- Invoice number, issue date, due date
- Bill To: customer name, email, address
- Line items table: description | qty | unit price | tax | total
- Subtotal, GST (10%), total
- Payment details note (businesses.payment_details or "Bank transfer: {BSB} {Account}")
- Footer: "Generated by Aria OS · ariaos.site"

Upload PDF to Supabase Storage bucket 'invoices' (create if not exists, public).
Store URL in invoices.pdf_url.
Commit: "feat(invoices): serverless-safe branded PDF generation → Supabase Storage"

## TASK 2 — Send invoice
POST /api/pos/invoices/[id]/send:
- Generate PDF if pdf_url empty or force=true param
- send_method='email': SendGrid, attach PDF, subject "Invoice {number} from {business_name} — Due {due_date}"
  - Email body: professional HTML template with payment summary and link to online view
- send_method='sms': Twilio SMS with short message + public view URL
- Update: sent_at=now(), status='sent', auto_reminders=true (default)
- If Xero connected and invoice has xero_invoice_id: sync status to Xero
Commit: "feat(invoices): send via email (PDF attached) or SMS + Xero status sync"

## TASK 3 — Recurring invoices
src/app/api/invoices/recurring/route.ts (check if exists, read it):
GET: list recurring invoice templates
POST { invoice_id, frequency: 'weekly'|'monthly'|'quarterly', start_date, end_date? }:
  Create recurring schedule. Merge processing into existing daily cron:
  - Find recurring invoices due today
  - Duplicate the invoice with new issue_date + due_date
  - Auto-send if auto_send=true
Commit: "feat(invoices): recurring invoice engine — weekly/monthly/quarterly"

## TASK 4 — Auto reminders + overdue
Merge into existing daily cron:
1. 7 days before due: send "friendly reminder" if auto_reminders=true
2. 1 day before due: send "due tomorrow" reminder
3. On due date if unpaid: update status='overdue', send overdue notice
4. 7 days overdue: send final notice, flag in Aria briefing

Track: reminder_count on invoices (add column if missing via apply_migration).
Commit: "feat(invoices/cron): smart reminder sequence — 7d, 1d, overdue, final"

## TASK 5 — Public invoice view (customer-facing)
src/app/invoice/[token]/page.tsx (no auth — public):
- Decode token → get invoice (use a secure token, not raw UUID)
- Show: business logo, invoice details, line items, total, due date, payment instructions
- "View PDF" button (opens pdf_url)
- Track: update invoices.viewed_at on first load
- "Mark as paid" button → POST /api/invoices/[token]/mark-paid (creates a payment record, owner confirms)
- Professional layout — this is customer-facing, must look polished

Generate token: jwt(invoice_id, business_id, exp: 90 days) — store in invoices.access_token column (add via migration if missing)
Commit: "feat(invoices): public customer invoice view with view tracking + mark paid"

## TASK 6 — Dashboard completeness
Audit src/app/dashboard/invoices/ — make every feature functional:
Stats bar: total outstanding | overdue amount | paid this month | avg days to pay
Status tabs: All | Draft | Sent | Overdue | Paid
Invoice table: number | customer | amount | status badge | due date | days outstanding | actions
Create invoice: customer picker (autocomplete from customers table), line item builder (+/- rows), notes, due date, send method, auto-reminders toggle
Invoice detail: PDF preview (iframe), Send button, Mark paid, Void, Duplicate, Download PDF
"Send reminder" manual button on overdue invoices
Xero sync indicator (if connected)

Feed into Aria briefing: overdue invoice count + total overdue amount.
Commit: "feat(invoices/dashboard): complete invoice UI — create, send, track, remind, PDF"

## Rules
- GST = 10% Australian standard — pre-fill tax_rate=10
- Amounts in dollars (numeric) not cents
- npx tsc --noEmit + npm run build before each commit
- All migrations via Supabase MCP
