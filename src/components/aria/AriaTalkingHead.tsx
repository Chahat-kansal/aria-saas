'use client';
// Avatar temporarily disabled — morph-target lip sync to be re-enabled post-launch
export default function AriaTalkingHead({ mode = 'idle', replyText = '' }: { mode?: string; replyText?: string }) {
  return null;
}
