export interface Service { id: string; name: string; duration_minutes: number; price: number | null; max_party_size: number; color: string; description: string | null; is_active?: boolean }
export interface Booking { id: string; customer_name: string; customer_phone: string | null; customer_email: string | null; booking_date: string; booking_time: string | null; party_size: number; duration_minutes: number; status: string; notes: string | null; aria_notes: string | null; reminder_sent_at: string | null; service_id: string | null; no_show_score: number | null; booking_token: string | null; booking_services: { name: string; color: string; duration_minutes: number } | null }
export interface Availability { id?: string; day_of_week: number; start_time: string; end_time: string; is_available: boolean; buffer_minutes: number; max_bookings_per_day?: number | null }
export interface BusinessHours { day_of_week: number; open_time: string | null; close_time: string | null; is_closed: boolean }
