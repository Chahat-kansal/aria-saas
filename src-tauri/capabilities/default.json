{
  "$schema": "../node_modules/@tauri-apps/cli/schema/capabilities-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Aria OS desktop. Future hardware access (receipt printers, cash drawers, card readers) will be added as Tauri plugins here.",
  "windows": ["main", "pos_kiosk"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-title",
    "core:window:allow-set-fullscreen"
  ]
}
